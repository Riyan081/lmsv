{
  "name": "ws-server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@repo/typescript-config": "*"
  },
    "scripts": {
    "build": "tsc -b",
    "dev": "bun run build && bun run start",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "better-auth": "^1.2.0",
    "@repo/db": "*",
    "@repo/auth": "*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
